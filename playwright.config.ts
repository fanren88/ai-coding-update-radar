import { defineConfig, devices } from "@playwright/test"; export default defineConfig({ testDir: "./tests/e2e", use: { baseURL: "http://127.0.0.1:3200", trace: "on-first-retry" }, webServer: { command: "npm run preview -- --port 3200", url: "http://127.0.0.1:3200", reuseExistingServer: !process.env.CI }, projects: [{ name: "chromium", use: { ...devices["Desktop Chrome"] } }, { name: "mobile", use: { ...devices["Pixel 7"] } }] });
